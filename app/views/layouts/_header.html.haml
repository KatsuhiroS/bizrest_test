%body
  %header
    .header-left
      %ul
        - if current_user
          %li
            = link_to 'Bizrest', root_path
          %li
            = link_to 'レッスン一覧', subjects_path
        - else
          %li
            = link_to 'Bizrest', root_path
    .header-right
      %ul
        - if current_user
          %li
            = link_to current_user.name, mypage_path
          %li
            = link_to 'ログアウト', signout_path, method: :delete
        - else
          %li
            = link_to 'ログイン', signin_path
          %li
            = link_to '新規登録', signup_path
